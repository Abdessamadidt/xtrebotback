export interface Coin {
        img: string;      // Image URL as a string
        price: number;    // Price as a number
        name: string;     // Name of the product as a string
        litname: string;  // Litname of the product as a string
      }

